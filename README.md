# Recipe Card Project

This project is a React application that displays recipe cards using reusable components and dynamic data. It demonstrates component structure, props, and clean file organization within the src folder.
